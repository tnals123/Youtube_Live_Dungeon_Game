# 던전 레이드 — 게임 설계 문서



## 지연시간 전제 (설계의 대원칙)

- 시청자 화면: 초저지연 설정 기준 2~5초 지연 (**방송 설정 필수**)
- 채팅 수집(pytchat): 1~3초 지연
- 판정 기준(서버 도달): 채팅 입력 후 **약 1~3초**
- 체감(내 행동이 화면에 보이기까지): 수집 1~3초 + 송출 2~5초 = **약 4~8초**
- → **모든 입력 창은 최소 20초.** 순발력 게임이 아니라
  "20~30초 동안 다같이 고민하고 치는" 게임으로 설계한다.
- 이 대기 시간이 곧 시청자 토론 시간 = 콘텐츠가 된다.

## 실현성 검토 메모 (규모)

- 서버 처리는 가벼움: 명령어 1건 = 딕셔너리 조회 수준. 초당 수백 건 OK.
- Socket.IO 클라이언트는 사실상 OBS 브라우저 소스 1개뿐 (시청자는 유튜브
  영상만 봄) → 접속 스케일 문제 없음.
- 실제 병목은 유튜브 채팅: 동시 시청 수천 명 규모에선 유튜브가 채팅을
  샘플링해서 일부만 전달. **비율제 판정이라 게임은 유지**되지만 개인 딜량
  집계는 일부 누락 가능.
- 화면 연출 주의: 공격 1건당 이벤트 1개 emit 구조는 수백 명 동시 입력 시
  로그 폭주. **0.5초 단위 배칭**(묶어서 집계 표시)으로 전환 필요.
- 결론: 동시 참여 수백 명까지 현 구조로 실현 가능.

## /내역할 — 역할 확인 오버레이 (필수 기능)

- 참가 카드가 4초 만에 사라지므로, 놓친 사람을 위한 확인 수단이 필수.
- pytchat은 **읽기 전용** → 봇이 유튜브 채팅에 답장 불가 (공식 API 답장은
  쿼터상 하루 ~200개뿐이라 실용성 없음). **답은 화면 오버레이 표시.**
- 동작: `/내역할` 입력 → 화면 고정 영역에
  "**ㅇㅇㅇ님은 ⚔️전사 (희귀) 입니다!**" 를 3초씩 큐로 순서 표시.
- **1인당 쿨타임 60초** (도배 방지). 동시 요청 많으면 2~3줄 묶어 표시.
- 보조 수단: 전투 로그에 역할 이모지 표시(구현됨), 보스전 화면에 역할별
  커맨드 가이드 상시 표시(구현됨).

## 명령어 표기 통일

- 모든 명령어는 `/` 접두사 기준 (`/참가`, `/내역할`, `/공격` ...).
- 시청자 실수 대비 `!` 접두사도 동일하게 처리 (`!참가` = `/참가`).

## 게임 루프

```
로비 (모집 60~90초)
  └ !참가 → 역할·등급 랜덤 배정 (구현됨)
던전 입장 브리핑 (파티 구성 요약, 구현됨)
┌─ N층 ──────────────────────────────────┐
│ 탐험 이벤트 1~2개 (각 20~30초 투표)      │
│   ├ 갈림길: /왼쪽 vs /오른쪽 → 다수결    │
│   ├ 상자:   /연다 vs /안연다 → 다수결    │
│   ├ 정답 → 무사 통과 (+가끔 보상)        │
│   └ 오답 → 함정 발동, 파티HP 감소        │
│ 보스전 (패턴 순환, HP 0까지)             │
│   └ 텔레그래프 → 입력 창 → 판정 → 반복   │
│ 층 클리어 (딜 랭킹 잠깐 표시)            │
└─ 다음 층으로 (신규 난입 !참가 허용) ─────┘
10층 클리어 → 던전 정복! 최종 결과 화면
전멸 (파티HP 0) → 결과 화면 → 로비로 리셋 + 재모집
```

- 층 사이 이동은 빠르게. "오늘 몇 층까지 갔다"가 자연스러운 방송 단위.
- 층이 올라갈수록: 보스 HP·피해량 증가, 패턴 수 증가, 신규 패턴 등장
  (기존 패턴 재활용 → 학습 보상).

## 던전 데이터 구조 (던전 1개 = JSON 1개, 서버에서 로드)

```json
{
  "id": "dark_catacomb",
  "name": "어둠의 지하묘지",
  "floors": [
    {
      "floor": 1,
      "explore": [
        {
          "type": "fork",
          "prompt": "갈림길이 나타났다. 왼쪽에서 바람소리가, 오른쪽에서 물소리가 들린다.",
          "options": ["/왼쪽", "/오른쪽"],
          "answer": "/왼쪽",
          "window_sec": 25,
          "on_correct": { "text": "안전한 길이다. 파티가 조용히 전진한다.", "reward": null },
          "on_wrong":   { "text": "바닥이 무너진다! 함정이다!", "party_damage": 3000 }
        },
        {
          "type": "chest",
          "prompt": "낡은 상자가 놓여 있다. 희미하게 빛이 새어나온다.",
          "options": ["/연다", "/안연다"],
          "answer": "/연다",
          "window_sec": 20,
          "on_correct": { "text": "축복의 부적! 다음 보스전 공격력 +20%", "reward": "atk_buff_20" },
          "on_wrong":   { "text": "상자를 지나쳤다. 아무 일도 없었다.", "party_damage": 0 }
        }
      ],
      "boss": {
        "name": "해골 기사",
        "emoji": "💀",
        "hp": 1000000,
        "patterns": ["earth_splitter", "vampiric_mist"],
        "pattern_interval_sec": 12
      }
    }
  ]
}
```

- `answer`는 **화면에 절대 표시하지 않는다.** 다수결 투표 결과가 정답과
  일치하는지로 판정.
- 투표 현황(양쪽 득표 막대)은 실시간으로 화면에 표시 — 긴장감 연출.
- 오답 상자(미믹), 안 여는 게 정답인 상자 등 배치로 변주.

## 보스 패턴 시스템 (핵심 신규 구현)

### 패턴 한 개의 흐름

```
1. 텔레그래프  "⚡ 보스가 대지를 향해 검을 치켜든다...!"   (화면 표시)
2. 입력 창     20~30초 카운트다운. 커맨드 수집
3. 판정        역할별 정답 응답률 → 성공 / 실패
4. 연출        성공: "방패벽이 충격을 막아냈다!"
              실패: "대지 가르기! 파티가 12,000 피해!" + 파티HP 감소
5. 평시        10~15초 자유 딜타임 (/공격 등이 딜로 들어감, 현재 구현 유지)
6. 다음 패턴   보스HP 구간·순서 규칙에 따라 선택
```

### 패턴 데이터 구조 (패턴 풀은 공용, 던전이 id로 참조)

```json
{
  "id": "earth_splitter",
  "name": "대지 가르기",
  "telegraph": "보스가 대지를 향해 검을 치켜든다...!",
  "window_sec": 25,
  "answers": {
    "warrior": ["/방어"],
    "archer": ["/회피"],
    "mage": ["/회피", "/보호막"],
    "healer": ["/회피"],
    "hero": ["/방어", "/회피"]
  },
  "success_threshold": 0.5,
  "on_success": { "text": "방패벽이 충격을 막아냈다!", "party_damage": 0 },
  "on_fail":    { "text": "대지가 갈라지며 파티를 덮쳤다!", "party_damage": 12000 }
}
```

### 판정 규칙 (구현 확정: 파훼 게이지 모델)

- 패턴 = **커맨드별 가중치 점수표** (양수 = 게이지 상승, 음수 = 하락)
- 게이지(0~100)는 **유효한 입력 하나하나가 무제한으로 절대 합산** (인원수로 나누지 않고,
  1인당 반영 횟수 제한도 없음 — 같은 사람이 같은 커맨드를 반복 입력해도 매번 그대로 더해짐).
  성공선(`success_threshold`) 이상이면 즉시 파훼.
  예: 0.5점짜리 정답 커맨드가 총 140번 입력되면 70점 도달 → 필요 횟수는
  점수 크기와 성공선으로 콘텐츠 제작자가 직접 설계
- **정답은 화면에 절대 노출 안 함** — 게이지의 실시간 반응으로 시청자들이 발견
- 실패 피해 = 최대 피해% × (1 − 게이지/100) — 아깝게 실패하면 살짝, 완전 오답이면 참사.
  실패해도 파훼율 %는 공개 → 다음 사이클의 힌트
- 입력 창 동안 마지막에 친 커맨드가 그 사람의 답 (정정 가능)

### 예시 패턴 (1층 보스 "해골 기사" 기준)

| 패턴 | 텔레그래프 | 파훼법 | 재미 포인트 |
|---|---|---|---|
| 대지 가르기 | "검을 치켜든다...!" | 전사 /방어, 나머지 /회피 | 기본기. 역할 분담 학습 |
| 흡혈의 안개 | "안개가 피 냄새를 풍긴다" | **전원 /공격 금지** (공격량만큼 보스 회복) | 반직관. 처음엔 다들 때리다 당함 |
| 마력 폭주 (2층~) | "붉은 마법진이 떠오른다" | 마법사 /역산 필수 | 역할 희소성 — 마법사 적으면 위기 |
| 절규 (3층~) | "보스가 비명을 지른다!" | 힐러 /정화 | 힐러 스포트라이트 |
| 광폭화 (HP 20% 이하) | "눈이 붉게 타오른다!!" | 전원 /공격 (딜 체크) | 마무리 화력전, 딜 랭킹 경쟁 |

## 파티 전투력 (구현 확정: 개인 사망 없음)

- 파티 자원 = **전투력** = 참가자 기여도(등급 배율)의 합 (브리핑 TOTAL POWER와 동일)
- 감소 요인: 패턴 실패, 탐험 함정, 몬스터 공격 — 전부 **최대 전투력의 %** 단위.
  "파티 일부가 쓰러졌다"는 설정이며 개개인은 죽지 않고 계속 참여
- 전투력이 깎인 비율만큼 **모두의 공격 데미지 감소** (하한 30%)
- /힐: 전투력 회복 (등급 배율 비례) — 힐러가 컴백의 열쇠
- **전투력 0 = 전멸 → 1층 리셋 + 재모집**

## 현재 코드 기준 구현 순서

1. **페이즈 자동 흐름** — dungeon → boss → 층 클리어 → 다음 층 / 전멸 리셋이
   타이머로 자동 진행 (`game_timer` 확장. 지금은 보스 진입이 수동 API).
2. **던전 JSON 로더 + 탐험 투표** — 투표 집계, 정답 판정, 파티HP 반영,
   투표 막대 UI (DungeonScene 신규).
3. **보스 패턴 엔진** — 텔레그래프 emit, 입력 수집, 비율 판정, 파티HP 반영.
   BossScene에 텔레그래프·카운트다운·판정 연출 추가.
4. **힐러/사망 메커니즘** — /힐, /부활, 개인 사망 처리.
5. **/내역할 오버레이 + 공격 로그 배칭** — 역할 확인 UI(쿨타임 60초),
   화면 이벤트 0.5초 배칭.
6. **리허설 도구** — 가짜 시청자 봇(N명이 랜덤/정답 커맨드를 치는
   시뮬레이터)으로 방송 전 전체 루프 테스트.
7. **콘텐츠 제작** — 첫 던전 1~10층 데이터 작성, 밸런스 조정.

## 기술 리스크 메모

- **pytchat 0.5.5는 비공식 라이브러리** — 유튜브 내부 변경으로 언제든 깨질
  수 있다. 채팅 수신부(`youtube_chat_listener`)를 인터페이스로 분리 유지,
  문제 시 `chat-downloader` 또는 공식 YouTube Data API(liveChatMessages
  폴링)로 교체.
- 방송 설정에서 **초저지연(ultra-low latency)** 필수.
- OBS는 `stream.html`(1080×1920 세로)을 브라우저 소스로 캡처.
